/*
 * Test configuration for CREATE_INPUT_CHANNEL subworkflow tests
 */

process {
    cpus   = 1
    memory = 2.GB
    time   = 1.h
}

params {
    outdir = 'results'
    publish_dir_mode = 'copy'
    sample_dir = null
    local_input_type = null
    convert_dotd = false
}
