/*
 * Test configuration for FRAGPIPE_DATABASE subworkflow tests
 */

process {
    cpus   = 2
    memory = 8.GB
    time   = 1.h

    withName: 'PHILOSOPHER_DATABASE' {
        ext.args = '--reviewed --contam'
    }
}

params {
    outdir = 'results'
    publish_dir_mode = 'copy'
}
